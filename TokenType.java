package com.nirvanaself.pluto;

public enum TokenType {
    BYTE,
    INT,
    DOUBLE,
    IDENTIFIER,
    LET,
    PLUS,
    MINUS,
    MULTIPLY,
    DIVIDE,
    MOD,
    LPAREN,
    RPAREN,
    SEMICOLON,
    EOF;
}