package com.nirvanaself.pluto;

public enum TokenType {
    BYTE,
    INT,
    DOUBLE,
    IDENTIFIER,
    VAR,
    PLUS,
    MINUS,
    MULTIPLY,
    DIVIDE,
    MOD,
    LPAREN,
    RPAREN,
    SEMICOLON,
    EOF;
}