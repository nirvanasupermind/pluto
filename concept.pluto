class Point {
    function constructor(x, y) {
        this.x = x;
        this.y = y;
    }

    function norm() {
        return Math.sqrt(this.x * this.x + this.y * this.y);
    }
}

Stdout.writeLine(Point(2, 2).norm());