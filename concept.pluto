# This does not run yet

var list = ["a", "b", "c"];

var first = Thread(lambda () {
    for(var i = 0; i < list.size(); i = i + 1;) {
        System.write(first.hashCode(), " ", list.get(i), "\n");
    }
});

var second = Thread(lambda () {
    for(var i = 0; i < list.size(); i = i + 1;) {
        System.write(second.hashCode(), " ", list.get(i), "\n");
    }
});

first.start();
second.start();