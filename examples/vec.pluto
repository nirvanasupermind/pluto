class Vec {
    def constructor(x, y) {
        self.x = x;
        self.y = y;
    }

    def magnitude() {
        return Math.sqrt(self.x * self.x + self.y * self.y);
    }
}

var vec = Vec(2.0, 3.0);

println(vec.magnitude()); # => 13.0