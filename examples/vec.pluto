class Vec {
    def constructor(x, y) {
        self.x = x;
        self.y = y;
    }

    def magnitude() {
        return Math.sqrt(self.x * self.x + self.y * self.y);
    }
}

var vec = Vec(2, 3);

println(vec.magnitude()); # 13