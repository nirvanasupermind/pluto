class Vec {
    def constructor(x, y) {
        self.x = x;
        self.y = y;
    }

    def magSquared() {
        return (self.x * self.x) + (self.y * self.y);
    }
}

var vec = Vec(2, 3);
var magSquared = vec.magSquared();

Stdout.write(vec, "\n");
Stdout.write(magSquared, "\n");